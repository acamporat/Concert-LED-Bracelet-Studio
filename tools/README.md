# Analysis tools roadmap

The initial implementation keeps receive and transmit paths separate and ships
passive `capture` plus `.sub` timing/protocol inspection. IQ envelope detection,
pulse clustering, capture-to-`.sub` reconstruction, plotting, and command
matching are Phase 2 items in `docs/IMPLEMENTATION_PLAN.md`.
