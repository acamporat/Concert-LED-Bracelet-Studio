# Captures

Store local HackRF receive recordings here. `*.cs8` files are ignored because
they grow at `2 * sample_rate` bytes per second (for example, 16 MB/s at 8 MHz).

Do not commit event recordings until their provenance, contents, and size have
been reviewed.
